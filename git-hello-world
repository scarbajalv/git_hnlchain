#!/bin/bash

git remote set-url origin git+ssh://git@github.com/scarbajalv/hello-world.git

git add .

echo "Insert Commit Message: "

read commitmsg

echo $commitmsg

NEWMSG="hola que tal"

git commit -m "$NEWMSG"

git push origin master

echo SUCCESS!

#algunas ediciones !!
